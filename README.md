# PocketBase Backend Service

This branch contains the backend database configuration, schemas, migrations, and database instance files.

## How to Run

1. Run the pocketbase.exe executable on Windows:
   `.\pocketbase.exe serve`
2. Open the Admin Panel at:
   `http://127.0.0.1:8090/_/`
